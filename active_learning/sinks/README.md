# Sinks

Output helpers for turning a `SelectionResult` into files or downstream side effects.

- `mosaic.py`: plain and overlay mosaic rendering.
- `sama.py`: composite CRID-export-plus-Sama submission sink.
- `yaml.py`: interactive seed payload export.

Sinks are the final step after selection has already been decided.
