# Sama Integration

Minimal helpers for sending exported selections to Sama.

- `seed.py`: creates a Sama batch from an export ID and image URL list.

This package intentionally stays small and depends on CRID export output rather than selection internals.
